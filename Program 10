// A program to multiply two complex numbers
#include <iostream>
using namespace std;
int main()
{
	float a,b,c,d;
	cout<<"Enter values of a,b,c,d: ";
	cin>>a>>b>>c>>d;
	cout<<"The result is: "<<(a*c-b*d)<<"+"<<(a*d+b*c)<<"i";
	return 0;
}
